<%= link_to "Create RFI", new_rfi_path %>

<% @rfis.each do |rfi, match| %>
  <%= render :partial => "display_rfi_heading", :locals => { :rfi => rfi, :match => match } %>
  <%= render :partial => "display_attachments", :locals => { :rfi => rfi } %>
<% end %>

